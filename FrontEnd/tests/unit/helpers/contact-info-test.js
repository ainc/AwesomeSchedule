import { contactInfo } from '../../../helpers/contact-info';
import { module, test } from 'qunit';

module('Unit | Helper | contact info');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = contactInfo(42);
  assert.ok(result);
});
