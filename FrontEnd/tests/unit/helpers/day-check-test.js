import { dayCheck } from 'awesome-schedule-front/helpers/day-check';
import { module, test } from 'qunit';

module('Unit | Helper | day check');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dayCheck([42]);
  assert.ok(result);
});
