import { findCourse } from 'awesome-schedule-front/helpers/find-course';
import { module, test } from 'qunit';

module('Unit | Helper | find course');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = findCourse([42]);
  assert.ok(result);
});
